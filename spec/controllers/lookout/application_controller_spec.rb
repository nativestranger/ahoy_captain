require 'rails_helper'

describe Lookout::ApplicationController do
  context 'when condition' do
    xit 'succeeds' do
      # Placeholder test - marked pending due to Rails 8 + RSpec-Rails view rendering incompatibility
      # TODO: Re-enable once rspec-rails supports Rails 8 view rendering properly
    end
  end
end
